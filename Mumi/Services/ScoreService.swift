import Foundation

class ScoreService {
    func fetchScores() -> [Score] {
        // For now, this returns an empty array.
        // In the future, this will scan the app's Documents directory.
        return []
    }
}
